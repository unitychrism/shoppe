.vendorSearch{:style => action_name == 'search' ? "display:block" : ''}
  = search_form_for @query, :url => search_vendors_path, :html => { :method => :post } do |f|
    %dl.left
      %dt= f.label :first_name_or_last_name_or_company_cont, t('shoppe.vendors.first_or_last_name')
      %dd= f.text_field :first_name_or_last_name_or_company_cont
      %dt= f.label :company_cont, t('shoppe.vendors.company')
      %dd= f.text_field :company_cont
    %dl.right
      %dt= f.label :email_cont, t('shoppe.vendors.email')
      %dd= f.text_field :email_cont
      %dt= f.label :phone_cont, t('shoppe.vendors.phone')
      %dd= f.text_field :phone_cont
      %dd= f.submit t('shoppe.vendors.search'), :class => 'button green button'
